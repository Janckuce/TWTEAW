
AUS_balance_of_power_category = {
	DEBUG_AUS_deactivate_BOP = {
		priority = 91 # Appears at the top (unless one has a higher priority)
		visible = { # Only visible in debug mode
			is_debug = yes
		}
		available = {
			original_tag = AUS
		}
		complete_effect = {
			remove_power_balance = { id = AUS_power_balance }
		}
	}
	AUS_support_left_side = {
		icon = GFX_decision_hol_exchange_intelligence_data # Icon on the left of the text
		visible = { # Always visible (might not be needed)
			always = yes
		}
		 days_remove = 35
		 cost = 20
		available = {
		}
		remove_effect = {
			add_power_balance_value = {
				id = AUS_power_balance
				value = -0.1 # Increases the power balance towards the left side
			}
		}
			
		}
		AUS_support_left_side_two = {
		icon = GFX_decision_generic_arrest # Icon on the left of the text
		visible = { # Always visible (might not be needed)
			always = yes
		}
		 days_remove = 35
		 cost = 30
		available = {
		}
		remove_effect = {
			add_power_balance_value = {
				id = AUS_power_balance
				value = -0.15 # Increases the power balance towards the left side
			}
		}
	}
	AUS_support_left_side_three = {
		icon = GFX_decision_generic_political_discourse # Icon on the left of the text
		visible = { # Always visible (might not be needed)
			always = yes
		}
		 days_remove = 35
		 cost = 40
		available = {
		}
		remove_effect = {
			add_power_balance_value = {
				id = AUS_power_balance
				value = -0.2 # Increases the power balance towards the left side
			}
		}
	}
	AUS_support_left_side_four = {
		icon = GFX_decision_category_generic_crisis # Icon on the left of the text
		visible = { # Always visible (might not be needed)
			always = yes
		}
		 days_remove = 35
		 cost = 50
		available = {
		}
		remove_effect = {
			add_power_balance_value = {
				id = AUS_power_balance
				value = -0.25 # Increases the power balance towards the left side
			}
		}
	}
}